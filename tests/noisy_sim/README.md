# Noisy Sim

This directory holds sim fixtures that intentionally produce uncertain recognition or recovery behavior.

The initial Sprint 2 fixture focuses on a low-confidence startup scan that retries and then escalates to `REVIEW_REQUIRED`.
